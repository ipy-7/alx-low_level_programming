#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - wtwwwwwww
 * @c: the character to check
 *
 * Description: WWWWWWWWWWW
 *
 * Return: 1 if the character is in lowercase, otherwise 0
 */
int print_last_digit(int c)
{
	c = _abs(c);
	printf("%d", c % 10);
	return (c % 10);
}
