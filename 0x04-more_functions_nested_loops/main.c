#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_square(0);
	print_square(4);
	print_square(2);
	print_square(1);
	return (0);
}
