#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_triangle(0);
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);

	return (0);
}
